import {FC} from 'react';
import Slider from "../component/Slider";

const MaListe: FC<{}> = ({}) => {
    return (
        <div>
            <Slider/>
        </div>
    );
};

export default MaListe;
